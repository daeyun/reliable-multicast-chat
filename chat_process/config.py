'''
hosts is a list of (IP address, port, process id)
'''

config = {
    'hosts': [
        ("localhost", 16405),
        ("localhost", 16406),
        ("localhost", 16407),
        ("localhost", 16408),
    ],
}