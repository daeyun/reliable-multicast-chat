"""
hosts is a list of (IP address, port)

ordering can be either 'casual' or 'total'
"""

config = {
    'hosts': [
        ('localhost', 16400),
        ('localhost', 16401),
    ],
    'ordering': 'casual',
}
